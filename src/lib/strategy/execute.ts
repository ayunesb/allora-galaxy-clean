export async function runStrategy(strategy_id: string) {
  // simulate logic (mock plugin execution, log, etc.)
  return {
    success: true,
    strategy_id,
    message: 'Strategy executed successfully',
  }
}
