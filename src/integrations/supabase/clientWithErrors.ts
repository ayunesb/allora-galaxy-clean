// This file needs updates to fix build errors, but I can't modify it directly
// as it's marked as read-only. We should create a new version with fixes when needed.

// This is just a patch to fix the immediate build errors
import { handleError, handleSupabaseError } from "@/lib/errors/ErrorHandler";
export { handleError, handleSupabaseError };
