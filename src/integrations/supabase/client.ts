
// Re-export from the standard Supabase client for backward compatibility
import { supabase, supabaseWithErrorHandler, isAuthenticated, getCurrentUserId } from '@/lib/supabase';
export { supabase, supabaseWithErrorHandler, isAuthenticated, getCurrentUserId };
