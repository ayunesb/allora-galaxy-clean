export function App() {
  return (
    <div>
      <h1>Hello, World!</h1>
    </div>
  ); // Ensure this closing parenthesis is present
}

// Add this so main.tsx can import default
export default App;